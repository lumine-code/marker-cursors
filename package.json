{
  "name": "marker-cursors",
  "author": "lumine-code",
  "description": "Show cursor positions and selections on the scrollbar and minimap.",
  "repository": "https://github.com/lumine-code/marker-cursors",
  "bugs": {
    "url": "https://github.com/lumine-code/marker-cursors/issues"
  },
  "keywords": [
    "overview-ruler",
    "gutter",
    "annotation",
    "decoration",
    "scrollmap",
    "minimap",
    "multi-cursor",
    "selection"
  ],
  "version": "1.1.0",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "providedServices": {
    "marker.layer": {
      "versions": {
        "1.0.0": "provideMarkerLayer"
      }
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.2.2",
    "globals": "^17.7.0",
    "prettier": "^3.9.6"
  },
  "configSchema": {
    "threshold": {
      "title": "Threshold",
      "description": "Hide markers if count exceeds this value. Zero means no limit.",
      "type": "integer",
      "minimum": 0,
      "default": 0
    },
    "showAll": {
      "title": "Show all cursors",
      "description": "Display all cursors or only the last one.",
      "type": "boolean",
      "default": true
    },
    "showSelections": {
      "title": "Show selections",
      "description": "Display selected ranges as full width markers.",
      "type": "boolean",
      "default": true
    },
    "inactiveShow": {
      "title": "Show in inactive editors",
      "description": "Display cursor markers in editors that are not currently focused.",
      "type": "boolean",
      "default": true
    }
  }
}
